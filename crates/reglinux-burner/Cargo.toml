[package]
name = "reglinux-burner"
version = "0.1.0"
edition.workspace = true
license = "MIT"
build = "build.rs"

[dependencies]
slint = "1.6"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
reglinux-fetch = { path = "../reglinux-fetch" }
usbimager-sys = { path = "../usbimager-sys" }

[build-dependencies]
slint-build = "1.6"
